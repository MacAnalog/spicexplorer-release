# ★ SUPER-DSL manifest (plan D-3). Validated against _shared/schema/circuit.schema.json.
# PROMOTED from the ferrosim_opamp reference entry (ota_fd2s): authored a device-generic
# abstract netlist + open-PDK bindings so the fully-differential two-stage is lowerable,
# assembled and simulated. The original TSMC 28nm decks + spectre run set are retained under
# `references`. The upstream IBIAS port and VCMR (CM reference) port are INTERNALIZED as ideal
# sources (sizing knobs x_ibias_val / x_vcmr_val) so the entry matches the amplifier class's
# self-biased differential-output convention (dc_op_diff/ac_open_loop_diff templates).
schema: spicexplorer/circuit@1
id: amp_023_fer_fd2s
class: amplifier
display_name: "Fully-differential 2-stage: NMOS folded-cascode + CS, Miller + RC-CMFB"
compensation: miller_nulling
stages: 2
realization: transistor
cm_control: regulated
signal_path: fully_differential
ports: [vinp, vinn, voutp, voutn, vdd, vss]
provenance:
  source: Arcadia-1/ferrosim (imported via netlist-crawler)
  aliases: [opamp, ferrosim_opamp, ota_fd2s]
  designer: Token Zhang
  license: MIT
artifacts:
  abstract_netlist: abstract/netlist.spice
  graph: abstract/topology.cgraph.json
  datasheet: datasheet.yaml
pdks: [ihp-sg13g2, sky130, gf180mcu, tsmc-n65]
analyses: [ac_open_loop, dc_op, noise, tran_step, thd, iip3, stb, cmrr_vcm, psrr_vdd]
references:
  - {tool: spectre, dir: spectre/28nm, node: 28nm}
optimize:
  budget: 2000
  knobs_from: pdk/{pdk}/sizing.yaml
  targets_from: datasheet.yaml
status: draft
