# GENERATED by `analog-db generate` — DO NOT EDIT.
# Source: optimizer/projection.yaml (extends circuit.yaml)
#   + pdk/ihp-sg13g2/sizing.yaml (dut_params) + datasheet.yaml (target_specs).
project:
  name: "PAM4-SIGE-DRIVER \xB7 analog-db drv_001"
  description: "DB projection of drv_001_pam4_sige_dac \u2014 2-bit PAM-4 SiGe HBT DAC driver sizing in ihp-sg13g2\
    \ (\xA73 spec tier)"
  simulator: ngspice
  save_sim: false
  ws_root: pdk/ihp-sg13g2
  netlist: testbenches/tb_bias.spice
  outdir: testbenches/temp_spice_out
  tech_spec:
    name: ihp-sg13g2
    constraints:
      min_nx: 1
      max_nx: 10
      ic_per_nx_ma: 3
      max_tail_ma_per_nx: 6
      vce_max: 2.0
      max_vcc: 4.0
  testbenches:
  - name: tb_gain_lsb
    netlist: testbenches/tb_gain_lsb.spice
    enable: true
  - name: tb_gain_msb
    netlist: testbenches/tb_gain_msb.spice
    enable: true
  - name: tb_s11
    netlist: testbenches/tb_s11.spice
    enable: true
  - name: tb_s22
    netlist: testbenches/tb_s22.spice
    enable: true
  - name: tb_bias
    netlist: testbenches/tb_bias.spice
    enable: true
  - name: tb_swing
    netlist: testbenches/tb_swing.spice
    enable: true
  optimizer_config:
    type: nevergrad
    random_seed: 48
    budget: 1000
    name: NGOpt
    target_specs:
    - name: lsb_gain_lf_db
      description: Differential power-wave S21 (2*Vout/Vsrc into 50 ohm/side) of the LSB cell, at 1 GHz.
      testbench: tb_gain_lsb
      sim_type: ac
      goal: exceed
      target: 2.2
      range: 6
      tolerance: 0.2
      weight: 100.0
      error_type: relative-sigmoid
      enable: true
    - name: msb_gain_lf_db
      description: Differential S21 of the MSB (2-cell) path, at 1 GHz. The headline gain.
      testbench: tb_gain_msb
      sim_type: ac
      goal: exceed
      target: 8.2
      range: 6
      tolerance: 0.2
      weight: 100.0
      error_type: relative-sigmoid
      enable: true
    - name: lsb_gain_50g_rel_db
      description: LSB S21(50 GHz) - S21(1 GHz). The -3 dB bandwidth requirement as a two-point check.
      testbench: tb_gain_lsb
      sim_type: ac
      goal: exceed
      target: -3.0
      range: 6
      tolerance: 0.3
      weight: 100.0
      error_type: relative-sigmoid
      enable: true
    - name: msb_gain_50g_rel_db
      description: MSB S21(50 GHz) - S21(1 GHz).
      testbench: tb_gain_msb
      sim_type: ac
      goal: exceed
      target: -3.0
      range: 6
      tolerance: 0.3
      weight: 100.0
      error_type: relative-sigmoid
      enable: true
    - name: s11_msb_32g_db
      description: "S11 of the input port (Zin from Vin/Iin, 50 ohm/side reference) at 32 GHz, the binding S11 point.\
        \ Constraint <= -10 dB; `minimize` keeps rewarding lower \u2014 this is the knob that lets a black-box search\
        \ rediscover the 50 ohm termination from the return-loss spec alone (\xA76)."
      testbench: tb_s11
      sim_type: ac
      goal: minimize
      target: -10.0
      range: 20
      tolerance: 0.5
      weight: 100.0
      error_type: relative-sigmoid
      enable: true
    - name: s22_50g_db
      description: S22 of the output port (Zout from the driven output, 50 ohm/side reference) at 50 GHz.
      testbench: tb_s22
      sim_type: ac
      goal: minimize
      target: -10.0
      range: 20
      tolerance: 0.5
      weight: 100.0
      error_type: relative-sigmoid
      enable: true
    - name: power_mw
      description: Total power drawn from the 4 V rail, i(Vcc)*4 in mW, at the bias operating point.
      testbench: tb_bias
      sim_type: op
      goal: minimize
      target: 192.0
      range: 100
      tolerance: 2.0
      weight: 100.0
      error_type: relative-sigmoid
      enable: true
    - name: ic_msb_ma
      description: 'Per-device collector current in an MSB cell (~itail/2, so ~8 mA at the golden tail), mA. A `.spiceinit`/ngbehavior
        liveness canary (feasibility gap 4.3): without `ngbehavior=hsa` the HBT conducts 0 A silently, so a dead
        deck reads ~0 mA and FAILS this >= 1 mA conducting floor rather than scoring 0 mW power as "best". Kept
        a FLOOR, not a band, so it never constrains the tail-current knob (which legitimately sets I_C from ~2 to
        ~12 mA).'
      testbench: tb_bias
      sim_type: op
      goal: exceed
      target: 1.0
      range: 8
      tolerance: 0.5
      weight: 10.0
      error_type: relative-sigmoid
      enable: true
    - name: swing_vpp_diff
      description: Large-signal differential output swing (Vpp) with LSB+MSB driven in phase at 1 GHz.
      testbench: tb_swing
      sim_type: tran
      goal: exceed
      target: 2.1
      range: 2
      tolerance: 0.1
      weight: 100.0
      error_type: relative-sigmoid
      enable: true
  dut_params:
  - name: x_dut_nx
    min_val: 1
    max_val: 10
    is_integer: true
  - name: x_dut_itail_ma
    min_val: 4
    max_val: 24
  - name: x_dut_re
    min_val: 0.5
    max_val: 20
  - name: x_dut_cdeg_ff
    min_val: 5
    max_val: 500
  - name: x_dut_rc
    min_val: 25
    max_val: 100
  - name: x_dut_rb
    min_val: 25
    max_val: 100
  - name: x_dut_vcasc
    min_val: 2.8
    max_val: 3.6
  - name: x_dut_vcm_in
    min_val: 1.6
    max_val: 2.2
  - name: x_dut_vcc
    min_val: 4.0
    max_val: 4.0
    freeze: true
    val: 4.0
