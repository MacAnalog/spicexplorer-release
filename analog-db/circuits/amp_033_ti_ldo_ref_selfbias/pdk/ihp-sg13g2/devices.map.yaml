# Generic device kind -> ihp-sg13g2 device name. Cross-PDK binding generated by
# `analog-db add-binding` (per-topology override of circuitgraph's built-in table).
pdk: ihp-sg13g2
devices:
  nmos: {model: sg13_lv_nmos}
  pmos: {model: sg13_lv_pmos}
