# DC load sweep; delta-Vout across the load range (volts).
schema: spicexplorer/analysis@1
id: load_regulation
template: load_regulation
description: "DC load sweep 0 -> 10 mA; delta-Vout across the load range."
params:
  VDD: 3.3
  ILOAD_START: 0
  ILOAD_STOP: 10m
  ILOAD_STEP: 0.5m
flags: []
produces: [load_reg]
