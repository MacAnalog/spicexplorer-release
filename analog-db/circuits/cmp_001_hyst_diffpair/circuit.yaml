# ★ SUPER-DSL manifest (plan D-3). Validated against _shared/schema/circuit.schema.json.
# PROMOTED from the ferrosim_hyst_comparator reference entry: authored a device-generic abstract
# netlist + open-PDK bindings so the hysteretic comparator is lowerable, assembled and simulated.
# The original proprietary-PDK 28/65 nm ferrosim decks are retained under `references`.
schema: spicexplorer/circuit@1
id: cmp_001_hyst_diffpair
class: comparator
display_name: "Continuous-time hysteretic comparator (R-tail pair + feedback trip-shifters)"
stages: 2
ports: [vdd, vout, vinp, vinn, vss]
provenance:
  source: Arcadia-1/ferrosim (imported via netlist-crawler)
  aliases: [hyst_comparator, ferrosim_hyst_comparator, dual-hysteresis-comparator]
  designer: Token Zhang
  license: MIT
artifacts:
  abstract_netlist: abstract/netlist.spice
  graph: abstract/topology.cgraph.json
  datasheet: datasheet.yaml
pdks: [ihp-sg13g2, sky130, gf180mcu]
analyses: [dc_op, dc_hysteresis]
references:
  - {tool: spectre, dir: spectre/28nm, node: 28nm}
  - {tool: spectre, dir: spectre/65nm, node: 65nm}
optimize:
  budget: 500
  knobs_from: pdk/{pdk}/sizing.yaml
  targets_from: datasheet.yaml
status: draft
