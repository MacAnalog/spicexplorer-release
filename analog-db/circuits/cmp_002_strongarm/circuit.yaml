# ★ SUPER-DSL manifest (plan D-3). Validated against _shared/schema/circuit.schema.json.
# PROMOTED from the ferrosim_comparator reference entry (plain StrongARM core): authored a
# device-generic abstract netlist + open-PDK bindings so the latched comparator is lowerable,
# assembled and simulated. The original proprietary-PDK 28 nm decks (incl. the offset-trim variant and the
# spectre MC/PSS characterization runs) are retained under `references`.
schema: spicexplorer/circuit@1
id: cmp_002_strongarm
class: comparator
display_name: "StrongARM latched comparator (clocked, differential out)"
stages: 1
ports: [vdd, voutp, voutn, vinp, vinn, clk, vss]
provenance:
  source: Arcadia-1/ferrosim (imported via netlist-crawler)
  aliases: [comparator, ferrosim_comparator, cmp_strongarm]
  designer: Token Zhang
  license: MIT
artifacts:
  abstract_netlist: abstract/netlist.spice
  graph: abstract/topology.cgraph.json
  datasheet: datasheet.yaml
pdks: [ihp-sg13g2, sky130, gf180mcu]
analyses: [tran_decision]
references:
  - {tool: spectre, dir: spectre/28nm, node: 28nm}
optimize:
  budget: 500
  knobs_from: pdk/{pdk}/sizing.yaml
  targets_from: datasheet.yaml
status: draft
