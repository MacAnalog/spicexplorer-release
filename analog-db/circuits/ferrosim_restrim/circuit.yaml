# ★ SUPER-DSL manifest — kind: reference (plan D-9). Validated against _shared/schema/circuit.schema.json.
# IMPORTED reference circuit: indexed by upstream POINTER only — no third-party or foundry-bound decks are redistributed here.
# The harness runs the reference-only T0 subset (schema + provenance + pointer) and skips T1-T4.
# Source credited under `provenance` (see repo NOTICE + corpora/ferrosim/PROVENANCE.md).
schema: spicexplorer/circuit@1
id: ferrosim_restrim
kind: reference
class: trim
display_name: 16-step thermometer resistor trim (ferrosim)
provenance:
  source: Arcadia-1/ferrosim (imported via netlist-crawler)
  designer: Token Zhang
  license: MIT
references:
- tool: spectre
  node: 28nm
  upstream: https://github.com/Arcadia-1/ferrosim
status: reference
