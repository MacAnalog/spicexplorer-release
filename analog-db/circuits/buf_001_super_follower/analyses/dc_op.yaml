# Analysis binding (plan D-4): buffer dc_op — operating point, level shift + supply current.
schema: spicexplorer/analysis@1
id: dc_op
template: dc_op
description: "DC operating point at the nominal VCM; v_out, v_offset + i_supply."
params:
  VDD: 1.5
  VCM: 0.9
  IBIAS: 10u
flags: []
produces: [v_out, v_offset, i_supply]
