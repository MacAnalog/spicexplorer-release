# dp_001_resistive_load — DUT parameterization (spicexplorer/params@1; contract: _shared/PARAMS.md).
# `devices:` is GENERATED from abstract/topology.cgraph.json — refresh with
# `analog-db gen-params --circuit dp_001_resistive_load --write` (it preserves the authored sections).
# `groups:`/`ratios:` are AUTHORED default tying, reviewed by a human (plan D-2):
# recommendations tagged with WHY, which an upstream user may accept, edit, or dissolve.
schema: spicexplorer/params@1
devices:
  RN: {Value: x_dut_rn_value}
  RP: {Value: x_dut_rp_value}
  XMN: {l: x_dut_xmn_l, m: x_dut_xmn_m, w: x_dut_xmn_w}
  XMP: {l: x_dut_xmp_l, m: x_dut_xmp_m, w: x_dut_xmp_w}
  XMR: {l: x_dut_xmr_l, m: x_dut_xmr_m, w: x_dut_xmr_w}
  XMT: {l: x_dut_xmt_l, m: x_dut_xmt_m, w: x_dut_xmt_w}
groups:
- name: pair_xmp_xmn
  kind: matched_pair
  members: [XMP, XMN]
  tie: [w, l]
  description: 'Mechanically derived from today''s sharing (P4 sweep) - legacy symbol(s): x_in_w (w),
    x_in_l (l).'
- name: mirror_xmr_l
  kind: mirror_length
  members: [XMR, XMT]
  tie: [l]
  description: 'Mechanically derived from today''s sharing (P4 sweep) - legacy symbol(s): x_tail_l (l).'
- name: shared_rp_value
  kind: shared_geometry
  members: [RP, RN]
  tie: [Value]
  description: 'Mechanically derived from today''s sharing (P4 sweep) - legacy symbol(s): x_rl (Value).'
