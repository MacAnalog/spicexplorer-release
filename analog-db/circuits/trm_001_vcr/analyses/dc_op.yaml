# Analysis binding (plan D-4): trim dc_op — output level + supply current at code 0.
schema: spicexplorer/analysis@1
id: dc_op
template: dc_op
description: "DC operating point (vcode=0); v_out + i_supply."
params:
  VDD: 1.2
flags: []
produces: [v_out, i_supply]
