# AnalogGym analysis binding (noise).
# Band standardized 2026-08-01 (campaign): FSTART 1k -> 1, FSTOP 1G — the paper's noise
# column is ONE uniform condition: total integrated input-referred noise, 1 Hz - 1 MHz,
# closed-at-DC bias config (this wrap), CL = 10p. PPD unchanged.
schema: spicexplorer/analysis@1
id: noise
template: noise_biaswrap
description: "Input-referred noise via the AnalogGym self-bias bias-wrap (Lfb/Cin)."
enabled: true
params: {VDD: 1.8, VCM: 240m, CL: 10p, FSTART: 1, FSTOP: 1MEG, PPD: 50}
flags: []
produces: [vn_in_integrated]
