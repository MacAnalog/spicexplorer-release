# SCAFFOLDED but UNSELECTED (enabled: false): the distilled ICMR sweep assembles (Tier 2)
# but the runner/sim tiers skip it until the tracking threshold is reviewed per-circuit.
schema: spicexplorer/analysis@1
id: linearity
template: linearity
description: "ICMR via buffer-connected DC sweep (distilled from the legacy cascode TB)."
enabled: false
params: {VDD: 1.5, VCM: 0.8, IBIAS: 5u, CL: 50f, VTRACK: 20m, VSWEEP_STEP: 0.01}
flags: []
produces: []
